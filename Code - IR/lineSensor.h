#include "globals.h"
#include "sense.h"

#define LN_SENSOR_PIN1 1
#define LN_SENSOR_PIN2 2

/* Reads value from the first line sensor (left?)
 * Returns analog value representing color sensor is over
 */
u16 getLineSensorValue1();


/* Reads value from the first line sensor (right?)
 * Returns analog value representing color sensor is over
 */
u16 getLineSensorValue2();
